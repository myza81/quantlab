# HANDOFF.md

Operational continuity document for QuantLab implementation sessions.
Optimized for fast AI-agent onboarding. Historical phase detail → `agent/archive/HANDOFF_HISTORY.md`.

---

# Current Platform State

## Identity

**QuantLab** — modular strategy research and execution ecosystem.
Architecture-first, governance-disciplined, AI-orchestrated development.

## Test Counts (as of Phase Strategy-UX-1A)

| Layer    | Tests  | Status |
|----------|--------|--------|
| Backend  | 4 847  | ✓ all pass (6 pre-existing polygon stale-path failures excluded) |
| Frontend | 561    | ✓ all pass |
| TypeScript | — | ✓ `tsc --noEmit` clean |
| Frontend build | — | ✓ `npm run build` clean (if run) |

## Backend Maturity

- **Auth**: JWT bearer tokens, bcrypt passwords, `UserRepository` (JSON-backed), three-tier roles (`user / admin / superadmin`), subscription lifecycle for regular users
- **Entitlement**: `require_active_subscription` (lazy expiry evaluation), `require_admin_role`, `require_superadmin_role`; `User.has_platform_access` is canonical discriminant
- **Ownership**: all resources (drafts, catalog entries, backtest runs, vault credentials) are user-owned; `user_id` always from JWT
- **Vault**: Fernet-encrypted credential storage per user; `VaultService` with ownership enforcement; `resolve_secret()` internal-only
- **Market data**: two-source model — Source A: external provider (Yahoo/Polygon via `ProviderAdapterFactory`) + vault credential resolver; Source B: local CSV/Parquet via dataset catalog
- **Dataset catalog**: `DatasetCatalog` (JSON registry); `file_path` backend-only (never in responses); `catalog_id` is sole durable identity
- **Provider abstraction**: `ProviderAdapterFactory` (len=4: yahoo, csv, parquet, polygon); `OHLCVService` (coverage-based incremental fetch + cache policies); `DatasetFetchIdentity` (SHA-256 fingerprint)
- **Strategy layer**: tool registry (SMA/EMA/RSI/MACD/ATR/Bollinger Bands), `StrategyDraft` + `DraftRepository`, semantic evaluation engine (`StrategySemantics` → `EvaluationPlan` → `evaluate_history`), backtest simulation (cost model, position sizing, equity curve, exports)
- **Admin governance**: `AdminService` (approve/suspend/reactivate/update-expiry/promote/demote); last-admin protection; self-suspension guard; no hard-delete path
- **Audit**: `emit_audit_event()` → structured JSON log to `quantlab.audit`; 63 `AuditEventKind` values (incl. `OVERSIZED_PAYLOAD_REJECTED`, `LIFECYCLE_TRANSITION_DENIED`, `POLYGON_ENV_FALLBACK_USED`, 30 `PT_*` paper trading events)
- **Security baseline**: `EnvironmentCredentialResolver`; request validation (`validate_date_range`, `validate_provider_type`, `validate_symbol`, `validate_bar_count`, `validate_uuid_id`); all provider errors sanitized before HTTP response
- **UUID path validation**: `validate_uuid_id()` in `request_validation.py`; applied at route handler level in `backtest_runs.py` (all 4 `{run_id}` routes) and `drafts.py` (all 4 `{draft_id}` routes); returns 400 for non-UUID path params before any file-path construction
- **Compute endpoint protection**: all 5 compute-heavy routes (`/semantics/evaluate-history`, `/semantics/evaluate-scalar`, `/semantics/extract-signal-events`, `/semantics/extract-trade-intents`, `/backtests/simulate`) require `require_active_subscription`
- **Payload guards**: `MAX_BACKTEST_BARS` (default 50 000) enforced on `/backtests/simulate` (price_bars) and `/backtests/runs` (bars); oversized → HTTP 400 + audit event; guard fires before any computation
- **Polygon ENV fallback gate**: `settings.polygon_allow_env_fallback` (default `False`); ENV-based API key resolution disabled by default; requires explicit opt-in; violation emits audit event
- **Strategy lifecycle**: `StrategyLifecycleStatus` enum (draft/validated/backtested/forward_tested/paper_tested/approved_for_live/archived); explicit transition table; `validate_lifecycle_transition()` enforced in draft service; `ARCHIVED` is terminal; invalid transitions emit `LIFECYCLE_TRANSITION_DENIED` audit event
- **Lifecycle promotion (Phase R3)**: `promote_draft_to_backtested(draft_id, run_id, repository, storage, owner_id, *, notes)` in `backend/api/services/draft_service.py`; requires completed backtest evidence (`run.draft_id == draft_id`, `run.status == "completed"`, same owner); validates lifecycle transition; emits `GOV_PROMOTION_REQUESTED`; exposed via `POST /backtests/runs/{run_id}/promote-draft` (request body: `draft_id`, optional `notes`); `LifecyclePromotionError` → 422; `DraftNotFoundError`/`BacktestAccessDeniedError`/`BacktestRunError` → 404; `PATCH /drafts/{id}` still exists but does NOT gate on backtest evidence — promotion path is the only evidence-gated route
- **Lifecycle UI (Phase R4)**: `LifecycleBadge` component (`frontend/src/components/LifecycleBadge.tsx`) — `data-testid="lifecycle-badge"` + `data-status={status}`, 7 status colors, optional `showNextStep` next-action hint; `lifecycle_status?: string` added to `StrategyDraftData`; `promoteDraftToBacktested(runId, draftId, notes?)` API client in `frontend/src/api/backtestRuns.ts`; `DraftDetailView` shows badge in header; `BacktestReportPage` shows promotion panel (test IDs: `promotion-panel`, `promote-to-backtested-btn`, `promotion-error`, `promotion-success`, `already-eligible-notice`, `forward-test-hint-btn`) when run completed + draft was `validated`; shows "already eligible" notice when draft was `backtested` or beyond; `App.tsx` wires `handlePromoteDraft → promoteDraftToBacktested`; lifecycle authority stays backend-authoritative
- **Backtest → Forward Test transition (Phase R5)**: `ForwardTestPrefill` type added to `types/forwardTesting.ts` (`draft_id`, `draft_name?`, `symbol?`, `timeframe?`, `provider_name?`, `exchange?`, `asset_class?`); `BacktestReportPage` now accepts `onStartForwardTest?: (prefill) => void` — `forward-test-hint-btn` enabled when prop present, `start-forward-test-btn` shown after promotion success; `ForwardTestPanel` accepts `prefill?: ForwardTestPrefill | null` + `onPrefillConsumed?: () => void` — auto-opens create form when prefill set, pre-populates `draft_id`/`symbol`/`timeframe`/`provider_name` from backtest context, shows `prefill-notice` with review hint for exchange/asset class, calls `onPrefillConsumed` on cancel or session creation; `App.tsx` adds `forwardTestPrefill` state + `handleStartForwardTest` (sets prefill + navigates) + `handlePrefillConsumed` (clears prefill); stale prefill not persisted in session storage; user must still review and submit creation form; backend lifecycle gate unaffected
- **Dependency injection**: `backend/api/dependencies.py` — canonical `get_draft_repository()`, `get_backtest_storage_path()`, `get_forward_test_repository()`, `get_forward_test_signal_store()`, `get_forward_test_bar_store()`, `get_paper_trading_repository()`, `get_paper_account_store()`, `get_account_state_snapshot_store()`, `get_paper_order_store()`, `get_paper_fill_store()`, `get_paper_position_store()`; all route modules import from there (no more duplicates)
- **Paper trading execution layer** (`backend/paper_trading/execution_models.py`, `execution_stores.py`, `broker_adapter.py`): `OrderDirection` / `PaperOrderStatus` / `ExecutionReason` / `RejectionReason` enums; `PaperOrder` (frozen, UUID guards, UTC enforcement, PENDING_FILL→FILLED/CANCELLED/REJECTED lifecycle via `model_copy`); `PaperFill` (frozen, self-documenting: gross_price + slippage + fill_price + gross_value + fee + net_value); `PaperPosition` (frozen, is_open invariants: open→quantity>0+closed_at=None, closed→closed_at set); `PaperOrderStore` (4 files per session: pending/filled/cancelled/rejected; `mark_filled` removes from pending + appends to filled + writes fill inline; dedup by fill_id); `PaperFillStore` (append-only, dedup by fill_id, owner filter); `PaperPositionStore` (one file per position, `save`/`update`/`load`/`list_positions`/`list_open_positions`/`count_open`, all UUID-guarded); `PaperBrokerAdapter` (pure stateless @staticmethod: `compute_fill_price` adverse slippage BUY↑/SELL↓, `compute_fee` FLAT/PERCENTAGE, `compute_quantity` FIXED_QUANTITY/EQUITY_FRACTION/FIXED_CASH, `create_pending_order`, `build_fill`, `reject_order` with quantity guard); architecture boundary: no imports from strategy_runtime/execution/backtesting/api/data_providers; 175 new tests (57 models + 63 stores + 55 adapter)
- **PaperTradingService** (`backend/paper_trading/service.py`): single-cycle execution engine; `PaperCycleResult` frozen dataclass (21 fields: session_id, status, bars_fetched, bars_processed, warmup_bars_processed, signal_eligible_bars_processed, signals_generated, signals_suppressed, pending_orders_resolved, orders_created, orders_rejected, fills_created, positions_opened, positions_closed, account_snapshot_created, last_processed_bar_timestamp, gap_detected, provider_failure, activated, drawdown_stop_triggered, message); `PaperTradingService.run_cycle(session_id, owner_id, identity, provider, *, now_utc)` — PENDING→`_activate()`, RUNNING→`_poll_cycle()`, PAUSED/terminal→no-op; `_activate()` derives warmup from strategy toolset via `derive_warmup_bars_required()`, fetches warmup bars via `get_recent_bars()`, stores with `is_warmup_bar=True`, cursor = last warmup ts or `now_utc`, PENDING→RUNNING transition, emits `PT_SESSION_ACTIVATED`; `_poll_cycle()` resolves NEXT_BAR_OPEN pending orders at bar.open (mandatory pre-signal step), then `get_bars_since(cursor)` + gap detection + full-window recomputation + `evaluate_history()` + signal processing (entry/exit long only) + `_mark_to_market()` per bar + drawdown stop check + `AccountStateSnapshot` per signal-eligible bar + cursor advance; long-only enforcement: BUY opens/scales position, SELL closes; SELL with no position → `NO_POSITION_TO_CLOSE` rejection; fill timing: `SIGNAL_BAR_CLOSE` fills immediately at bar.close, `NEXT_BAR_OPEN` creates `PENDING_FILL` order persisted across cycle boundary; account math: BUY deducts `net_value + fee`, SELL adds `net_value - fee`; mark-to-market: all open positions updated with bar.close → equity recalculation → drawdown update; drawdown stop: if `current_drawdown_pct >= max_drawdown_stop_pct` → session transitions to PAUSED + `PT_DRAWDOWN_STOP_TRIGGERED` audit; `ForwardTestBarStore`/`ForwardTestSignalStore` reused directly for PT session (cross-module allowed); reuses FT evaluation engine (`evaluate_history`, `compute_tool_outputs_for_history`) and strategy compiler; `RangeProviderAdapter` imported under `TYPE_CHECKING` only; no scheduler, no polling loop, no API routes, no frontend, no broker execution, no short selling; 69 new tests
- **Paper Trading API** (`backend/api/routes/paper_trading.py`, `backend/api/schemas/paper_trading.py`): 15 protected routes under `/paper-trading/sessions`; all require `require_active_subscription`; `user_id` always from JWT; wrong-owner → 404 (information hiding); `file_path`/`strategy_json`/`user_id` never in responses; no broker account identifiers in responses; session creation: draft ownership + lifecycle gate (>= forward_tested) + snapshot seal + SHA-256 hash + `PaperAccount` created atomically + `PT_SESSION_CREATED` audit; run-cycle: catalog rejection (422) + vault credential resolution + provider build + `DatasetIdentity` + `PaperTradingService.run_cycle()` dispatch; pause/resume/terminate: explicit lifecycle transition validation; **terminate** (Phase P8B): calls `apply_terminal_cleanup()` before status flip — cancels pending orders, closes open positions, finalizes account, appends final equity snapshot; signals/orders/fills/positions: ownership gate via repository then store list; account: `PaperAccountStore.load_by_session_id()`; equity-curve: `AccountStateSnapshotStore.list_snapshots()`; bars: `ForwardTestBarStore.list_bars()` (reused for PT session); **promote-draft** (Phase P7/P8C): `POST /{session_id}/promote-draft` → `promote_draft_to_paper_tested()` in draft_service; P8C evidence gates: (1) `session.last_processed_bar_timestamp is not None`, (2) `session.status in {TERMINATED, COMPLETED}`, (3) `snapshot_store.count(session_id) >= 1`; `snapshot_store` injected via `get_account_state_snapshot_store`; audit enriched with `session_status`/`equity_snapshot_count`; `PaperTestPromotionError` → 422; `PaperTradingSessionNotFoundError`/`DraftNotFoundError` → 404; returns `DraftResponse`; schemas: `SimulationAssumptionsRequest/Response`, `CreatePaperTradingSessionRequest`, `PaperStrategySnapshotResponse`, `PaperTradingSessionSummaryResponse`/`DetailResponse`, `PaperCycleResultResponse`, `PaperOrderResponse`, `PaperFillResponse`, `PaperPositionResponse`, `PaperAccountResponse`, `AccountStateSnapshotResponse`, `PaperBarResponse`, `PaperSignalResponse`, `PromoteDraftToPaperTestedRequest`; 101 backend route tests
- **Terminal cleanup** (`backend/paper_trading/terminal.py`): `apply_terminal_cleanup(session_id, account_id, owner_id, order_store, position_store, account_store, snapshot_store, now_utc)` — 4-step idempotent cleanup: (1) cancel PENDING_FILL orders with `cancellation_reason="session_terminated"`, (2) close open positions at last known mark price (no fill, unrealized P&L → realized), (3) finalize PaperAccount (status=CLOSED, cash recovered from positions, peak updated), (4) append final AccountStateSnapshot (open_position_count=0, unrealized=0); architecture boundary: no imports from strategy_runtime/execution/backtesting/api/data_providers; graceful return if no account exists; 23 unit tests in `test_paper_trading_terminal.py`
- **Config-driven paths**: `settings.drafts_storage_path`, `settings.backtest_runs_storage_path`, and `settings.forward_test_sessions_storage_path` replace all hardcoded `Path("storage/...")` patterns
- **Forward testing foundation** (`backend/forward_testing/`): `ForwardTestSession` (frozen Pydantic, 6-state lifecycle PENDING→RUNNING→PAUSED/COMPLETED/FAILED/TERMINATED, source_mode consistency, UUID field guards, UTC enforcement), `ForwardTestSignal` (immutable, no fills/positions/P&L, bar OHLCV snapshot), `ForwardTestBar` (warmup flag, bar_index, processed_at), `StrategySnapshot` (sealed strategy copy), `ForwardTestRepository` (JSON-backed, ownership-safe, UUID path guard, wrong-owner → `ForwardTestSessionNotFoundError`), `ForwardTestSignalStore` (idempotency: session_id + bar_timestamp + signal_direction), `ForwardTestBarStore` (idempotency: session_id + bar_timestamp; last timestamp queries); storage: `storage/forward_tests/sessions/`, `signals/`, `bars/`
- **Strategy lifecycle** (`backend/strategy_registry/lifecycle.py`): `StrategyLifecycleStatus.FORWARD_TESTED` added; canonical path `draft→validated→backtested→forward_tested→paper_tested→approved_for_live→archived`; `backtested→paper_tested` retained as deprecated transitional (exploratory, not promotion-eligible per `docs/STRATEGY_PROMOTION_LIFECYCLE.md §5`); `forward_tested→backtested` rollback allowed
- **Audit taxonomy** (`backend/core/audit.py`): 19 `FT_*` event kinds added (`FT_SESSION_CREATED`…`FT_SESSION_REVIEWED`); 9 `GOV_*` event kinds added (`GOV_PROMOTION_REQUESTED`…`GOV_LIFECYCLE_TRANSITION_DENIED`); `AuditEvent.correlation_id: str | None` added (optional, backward-compatible, serializes to JSON when present, absent from JSON when None); existing `LIFECYCLE_TRANSITION_DENIED` kept for backward compat alongside new `GOV_LIFECYCLE_TRANSITION_DENIED`
- **OHLCVService extensions** (`backend/services/ohlcv_service.py`): `timeframe_to_timedelta(timeframe) → timedelta` (all 15 canonical timeframes; "1M" = 30 days approximation); `is_bar_finalized(bar_timestamp, timeframe, *, current_time, buffer_seconds=60) → bool` (bar finalized when `current_time >= bar_open + tf_duration + buffer`); `OHLCVService.get_recent_bars(identity, limit, provider, *, reference_time, bar_finalization_buffer_seconds, lookback_multiplier)` (latest N finalized bars, BYPASS_CACHE, ascending); `OHLCVService.get_bars_since(identity, since_timestamp, provider, *, reference_time, bar_finalization_buffer_seconds)` (finalized bars strictly after cursor, BYPASS_CACHE, ascending); `settings.forward_test_bar_finalization_buffer_seconds = 60` added to config; no session coupling, no storage writes, no forming-candle evaluation
- **Market calendar** (`backend/market_calendar/`): `TradingCalendar` ABC; `TwentyFourSevenCalendar` (crypto/24-7, all bars expected); `WeekdayMarketCalendar` (Mon–Fri equity, weekends not expected, holiday DB deferred); `DefaultCalendar` (conservative fallback = WeekdayMarketCalendar); `get_calendar(asset_class, *, provider_name, exchange, symbol) → TradingCalendar` registry (crypto→24/7, equity→weekday, unknown→default, case-insensitive); `is_bar_expected(bar_timestamp, timeframe, calendar) → bool` (gap detection: market closure vs. provider failure); `is_bar_finalized(bar_timestamp, timeframe, now_utc, calendar, safety_buffer=60) → bool` (calendar-aware: time-finalized AND expected; wraps Phase 4C.3 time-check; Saturday equity bar always False); UTC-only enforcement throughout; no external calendar dependency; no strategy-layer imports; documented in `docs/MARKET_CALENDAR.md`
- **ForwardTestService** (`backend/forward_testing/service.py`): single-cycle evaluation engine; `CycleResult` frozen dataclass (session_id, status, bars_fetched, bars_processed, warmup_bars_processed, signal_eligible_bars_processed, signals_generated, signals_suppressed, last_processed_bar_timestamp, gap_detected, provider_failure, activated, message); `run_cycle(session_id, owner_id, identity, provider, *, now_utc)` — PENDING→`_activate()`, RUNNING→`_poll_cycle()`, PAUSED/terminal→no-op; `_activate()` — `validate_session_transition(PENDING→RUNNING)`, `get_recent_bars()` warmup (warmup_period bars), all stored with `is_warmup_bar=True`, cursor = last warmup bar timestamp or `now_utc`, audit `FT_SESSION_ACTIVATED`; `_poll_cycle()` — `get_bars_since(cursor)` + gap detection via `_calendar_is_bar_expected(expected_next_ts)` + full-window recomputation (stored bars + new bars via `compute_tool_outputs_for_history()`) + `evaluate_history()` on all bars + signal emit only for new bar indices + idempotent `append_bar/append_signal` + cursor advance to last new bar timestamp + session counter updates + audit `FT_POLL_COMPLETED`; `_prepare_strategy()` — `StrategyDraft.model_validate_json(strategy_json)` + `compile_semantics(draft.semantics, draft_id)` → `(draft, plan, error_message)`; `RangeProviderAdapter` imported only under `TYPE_CHECKING` guard (boundary: `forward_testing` must not import from `data_providers`); `DatasetIdentity` passed by caller (route layer sets exchange/asset_class); no scheduler, no polling loop, no background workers, no API routes, no frontend, no broker execution
- **Forward Testing API** (`backend/api/routes/forward_testing.py`, `backend/api/schemas/forward_testing.py`): 9 protected routes under `/forward-tests`; all require `require_active_subscription`; `user_id` always from JWT; wrong-owner → 404 (information hiding); `file_path`/`strategy_json`/`user_id` never in responses; session creation: draft ownership + lifecycle gate (>= backtested) + snapshot seal + warmup derivation + `FT_SESSION_CREATED` audit; run-cycle: catalog rejection (422) + vault credential resolution + provider build + `DatasetIdentity` + `ForwardTestService.run_cycle()`; pause/terminate: explicit pre-check that `resume` requires PAUSED status (PENDING→RUNNING allowed by state machine for activation only); DI providers `get_ohlcv_service`, `get_tool_registry`, `get_provider_factory` added to `backend/api/dependencies.py`; `ForwardTestSession` model extended with `credential_id`, `exchange`, `asset_class` (all defaulted, backward-compatible); 37 backend route tests in `tests/unit/test_forward_testing_routes.py`
- **Forward Testing Frontend** (`frontend/src/types/forwardTesting.ts`, `frontend/src/api/forwardTests.ts`, `frontend/src/components/ForwardTestPanel.tsx`): TypeScript types mirror backend schemas (no `strategy_json`, no `user_id` in summaries); `authedFetch` for all API calls; no `setInterval` auto-polling; MVP panel: session list with status badges, run-cycle/pause/resume/terminate buttons, signal history drill-in, create form (draft_id + symbol + timeframe + provider + exchange + credential); "Forward Test" nav tab added to `App.tsx`; 28 frontend tests (13 API + 15 component)

## Frontend Maturity

- **Auth**: `AuthContext` (JWT in localStorage, `authedFetch`, `refreshUser()`), `AuthGuard`, `LoginPage`, `RegisterPage`
- **Entitlement**: `SubscriptionGate` (blocks pending/expired/suspended users; admin passthrough)
- **Market data**: `Controls.tsx` — provider selector, credential selector (Polygon), Fetch button; `DatasetMetaBadge` (provenance strip)
- **Dataset catalog**: `CatalogManager.tsx` — list/register/remove/load; `CatalogMetaBadge`; `file_path` cleared after submit, never rendered; `handleCatalogLoad` in App.tsx switches to chart with catalog data
- **Credentials**: `CredentialManager.tsx` — list/add/disable/delete; secret cleared from state after API call
- **Strategy**: `DraftWorkspace.tsx` (full CRUD + archive), `SemanticEditorPanel.tsx`, `PlanInspectionPanel.tsx`, `StrategyTestPanel.tsx` (composition + backtest trigger + session context display + Composer shortcut)
- **Chart**: `Chart.tsx` — lightweight-charts v5, two-pane (price + oscillator), overlay lifecycle managed, `CatalogMetaBadge` / `DatasetMetaBadge` above chart
- **Research session**: `ResearchSession` type + `SessionProvenanceStrip.tsx` — persistent strip below nav showing active dataset/strategy/last-run; `no file_path` ever rendered in session context; Phase 3S-C adds "Resume Last Report" button (restore last run after page refresh)
- **Session persistence**: `useSessionPersistence` hook — sessionStorage (`ql_session_v1`), partial merge saves, path sanitization on load; never stores JWT, secrets, file_path, raw candle data; clears on window close
- **Report flow**: `BacktestReportPage.tsx` — source provenance label, "Edit Strategy" → Composer; "Report" nav tab appears once a backtest result exists; Phase 3S-C adds Run Provenance section (dataset + draft provenance with short-hash display)
- **Backtest History**: `BacktestHistoryPanel.tsx` — `GET /backtests/runs`, ownership-filtered, newest-first; "Reopen" fetches full report; loading/empty/error states; History nav tab (always visible)
- **Admin**: `AdminConsole.tsx` (approve/suspend/reactivate/update-expiry/promote/demote); superadmin-tier buttons gated on `isSuperadminViewer`
- **Lifecycle visibility (Phase R4)**: `LifecycleBadge` in `DraftDetailView` header (current status + next-step hint); `BacktestReportPage` Run Provenance shows `LifecycleBadge` for `lifecycle_status_at_run`; promotion panel conditionally rendered; `promoteDraftToBacktested` frontend API client; `already-eligible-notice` for backtested-or-beyond drafts
- **Backtest → Forward Test flow (Phase R5)**: `forward-test-hint-btn` enabled for backtested+ drafts when `onStartForwardTest` wired; `start-forward-test-btn` in promotion success state; `ForwardTestPanel` auto-opens prefilled create form from backtest context; `prefill-notice` guides user to review exchange/asset class; `ft-draft-id-input` / `ft-symbol-input` data-testids; `onPrefillConsumed` clears stale prefill from App state after cancel/create
- **Forward Test form upgrade (Phase R6)**: `CreateForm` in `ForwardTestPanel.tsx` now loads credentials (`fetchCredentials`) + eligible drafts (`fetchDrafts`) on mount; credential field is a `<select>` filtered by provider (`ft-credential-select`); `no-credentials-notice` shown when no credentials match provider; draft field is context-aware — read-only `ft-draft-display` in prefill mode, `ft-draft-select` picker for eligible (`ELIGIBLE_FT_STATUSES`) drafts in manual flow, raw input fallback during loading or empty list; no UUID copy-paste required in guided flow; pickers degrade gracefully on API failure; R5 prefill behavior and test compatibility preserved via hidden `ft-draft-id-input`
- **Forward Test operator view (Phase R7)**: `SignalList` replaced by `SessionOperatorView`; loads session detail + signals + bars in parallel; 6 panels: `config-panel` (configuration summary), `cycle-panel` (cycle metrics including warmup progress), `provenance-panel` (session/draft/snapshot IDs), `signals-panel` (signal trace with feature values column), `bars-panel` (recent bars table, newest-first, WARMUP/SIGNAL labels, capped at 50), `failure-panel` (shown when `failure_reason` set); `session-state-banner` with human-readable status message; actionable `no-signal-explanation` for all 6 session states — pending/paused/failed/terminated/completed/running-warmup/running-eligible-but-no-signals; signal trace adds `feature_values_at_signal` display; no backend changes required (all data in existing endpoints)
- **Strategy Lifecycle Dashboard** (`frontend/src/components/StrategyLifecycleDashboard.tsx`, Phase UX-2): central command center for strategy progression; draft picker (auto-selects first on load); lifecycle stage track (6 nodes: draft/validated/backtested/forward_tested/paper_tested/approved_for_live; `data-state=complete|current|locked`); current stage card; next-action card with deep-link button or locked message; blockers section; evidence summaries for each active stage (backtest runs, FT sessions, PT sessions); quick-navigation buttons to Composer/History/Forward Test/Paper Trading; collapsible technical details (draft ID, lifecycle enum, session IDs); all data via existing list APIs + client-side `draft_id` filtering; no lifecycle authority moved to frontend; no live trading; 34 frontend tests; now uses shared `computeGuidance` + `resolveNav`
- **Guided Next-Step Layer** (Phase UX-3): `lifecycleGuidance.ts` — shared `computeGuidance(GuidanceContext): GuidanceResult` (single source of truth for all lifecycle guidance); `NavigateTarget` string enum; `STAGE_LABELS` map; `LifecycleGuidanceCard` — reusable display-only card with testids `lgc-card/current-stage/next-action/why/blockers/blocker-item/no-blockers/navigate-btn/navigate-locked`; integrated into: `DraftWorkspace` (above DraftDetailView, lifecycle_status only), `BacktestReportPage` (before metrics, navigates to Composer if needed), `ForwardTestPanel.SessionOperatorView` (between state-banner and config-panel, uses lifecycle_status_at_activation + evidence bars), `PaperTradingPanel.SessionDetailView` (after action feedback, uses lifecycle_status_at_activation + PT evidence flags); 29 new tests; no lifecycle authority moved to frontend; backend remains authoritative
- **Terminology & Copy Cleanup** (Phase UX-4): user-facing labels updated throughout — `STAGE_LABELS` now maps `backtested→'Backtest Complete'`, `forward_tested→'Forward Test Complete'`, `paper_tested→'Paper Trading Evidence Complete'`, `approved_for_live→'Approved for Live Review'`; nav labels: `Strategy Builder` / `Backtest History` / `Forward Testing` / `Lifecycle Dashboard`; panel buttons: `'Process Next Bar'` (was `'Run Cycle'` in both FT and PT panels); panel labels: `'Market Data Source'` (was `'Provider'`), `'Strategy Stage at Start'` (was `'Lifecycle at Activation'`, now also maps value through STAGE_LABELS), `'Live Bars Evaluated After Warmup'` (was `'Signal-Eligible Bars'`); section headings: `'Advance Stage'` (was `'Lifecycle Promotion'`); promotion text uses user-facing labels (no raw `forward_tested`/`paper_tested` enum strings shown to users); `BacktestHistoryPanel` lifecycle badge now maps through `STAGE_LABELS`; `LifecycleBadge` STATUS_META updated consistently; `StrategyLifecycleDashboard` LIFECYCLE_STAGES now derived from shared `STAGE_LABELS`; all backend enum values (`backtested`, `forward_tested`, etc.) and API contracts unchanged; 383 tests pass
- **Nav**: Chart | Strategy Builder | Credentials | Datasets | Backtest History | Forward Testing | Paper Trading | Lifecycle Dashboard | Report (when backtest exists) | Admin (admin/superadmin only)
- **Paper Trading Panel** (`frontend/src/components/PaperTradingPanel.tsx`): session list with status badges; create form (draft picker filtered to `forward_tested`/`paper_tested`/`approved_for_live`, optional FT session picker filtered by draft + `signal_eligible_bars_processed > 0`, symbol/timeframe/provider/starting cash fields); **P4/P5/P6/P7/P8C SessionDetailView** — loads session/account/orders/fills/positions/equity plus current draft lifecycle; paper-tested promotion panel is shown only while current draft is still `forward_tested` and evidence exists, preventing stale post-refresh promotion actions; **P8C promotion panel states**: `pt-promotion-no-evidence` (no bars processed), `pt-promotion-not-finalized` (bars processed but session not yet terminated/completed — e.g. still running or paused), `promote-to-paper-tested-btn` (shown only when status is `terminated` or `completed` and bar evidence exists), `pt-promotion-success` (after successful promotion), `pt-promotion-error` (on backend rejection); backend is authoritative for snapshot-count gate; **Performance Analytics section** (P6) — `EquityCurveChart` (`pt-equity-curve`) SVG equity+cash lines with area fill, date range label, `pt-equity-curve-insufficient` (< 2 snapshots), `pt-equity-curve-error`; `DrawdownSummary` (`pt-drawdown-summary`) current+max drawdown, peak equity, latest equity; `ReturnSummary` (`pt-return-summary`) initial/latest/absolute/pct return with sign prefix; `TradeOutcomeSummary` (`pt-trade-outcome`) order counts, open/closed positions, `pt-no-completed-trades` when fills empty; `MetricsSummary` (`pt-metrics`) 10-cell operational grid (P5); `AccountDetailCard` (`pt-account-detail`) 11 balance fields (P4); Trade Ledger (`pt-trade-ledger`) with symbol/status filters and sort (P5); Equity History table (`pt-equity-history-table`) (P5); Positions/Orders/Fills raw tables (P4); action buttons (run-cycle/pause/resume/terminate); `pt-cycle-result` / `pt-action-error` / `pt-refresh-btn`; no scheduler, no setInterval, no auto-polling; no live trading; no new charting dependencies
- **Forward Test Panel**: `ForwardTestPanel.tsx` — session list with status badges (pending/running/paused/completed/failed/terminated), contextual action buttons (Activate/Run Cycle/Pause/Resume/Terminate), signal history drill-in via session ID link, create session form (draft_id, symbol, timeframe, provider, exchange, credential_id)

---

# Architecture Invariants — MUST NOT VIOLATE

## Identity & Ownership

- `user_id` **always** from `current_user.user_id` (JWT) — never from request body, query param, or client claim
- Wrong-owner access → HTTP **404** (same exception as not-found — information hiding)
- Legacy resources (`user_id=None`) inaccessible to all authenticated users

## Entitlement Model

- `require_admin_role` must **never** depend on `require_active_subscription` (admins manage users even after their own subscription expires)
- `User.has_platform_access`: admin/superadmin = `True` (role-based, subscription irrelevant); regular user = subscription-based
- Superadmin can promote `user→admin` and demote `admin→user`; regular admin cannot
- No self-promotion, no self-demotion (service guard + audit)
- No hard-delete user path anywhere (no endpoint, no button, no service method)

## Data Isolation

- `file_path` never in any API response (catalog service resolves internally)
- `encrypted_secret` never in any API response (vault service resolves internally)
- `password_hash` never in any API response (auth schema has no such field)
- `CredentialMetadataResponse` has no `secret_value`, `encrypted_secret`, or `raw_secret`

## Provider & Strategy Layer

- Strategies must never import from: `data_providers/`, `vault/`, `api/`, `storage/`, or any frontend module
- `market_data_service.py` has no concrete adapter imports (factory pattern only)
- Provider errors must be sanitized before HTTP responses (no file paths, no API key values)
- `resolve_secret()` in `VaultService` is internal-only — never called from routes

## Frontend

- All ownership-scoped endpoints use `authedFetch` (never raw `fetch`)
- `AuthError` on 401 → `logout()` → `AuthGuard` shows `LoginPage`
- `file_path` cleared from state in `resetForm()` immediately after successful catalog registration
- `catalog_id` is sole durable frontend resource identity for catalog entries

---

# Admin / Governance Model

```
superadmin  → is_admin=True, is_superadmin=True  (promote/demote, all admin actions)
admin       → is_admin=True, is_superadmin=False  (approve/suspend/reactivate/update-expiry)
user        → subscription-based access
```

**Bootstrap**: first registration matching `admin_bootstrap_email` config → `role=superadmin`.
**Migration**: `backend/scripts/promote_superadmin.py <email>` (idempotent; one-time for existing `role=admin` accounts).
**Subscription lifecycle**: `pending → active → expired/suspended → active`. Lazy expiry on every protected request.

---

# Completed Milestones

| Phase | Milestone |
|-------|-----------|
| Chart-UX-3C.2 | **Chart pane synchronization & indicator styling** — Three UX defects fixed. (1) User-editable colors: `seriesColors: Record<string, string>` added to `IndicatorInstance`; `handleSeriesColorChange()` in ChartIndicatorPanel updates `seriesColors` + `instanceColor` (for single-series) without triggering `computeArtifact`; inline color editor (`<input type="color">`) added below parameter fields in settings editor; App.tsx patches `artifact.series[i].default_color` from `seriesColors` overrides before passing to Chart, keeping Chart prop interface stable. (2) Time-scale synchronization: replaced `subscribeVisibleLogicalRangeChange`/`setVisibleLogicalRange` with `subscribeVisibleTimeRangeChange`/`setVisibleRange` throughout (price↔strategy-osc pane, price↔each OscPane); OscPane no longer calls `fitContent()` after data update — reads price chart's current range via `getVisibleRange()` and applies it instead, eliminating horizontal drift between panes. (3) Crosshair sync: OscPane subscribes to `priceChart.subscribeCrosshairMove`; maintains a `timeValueMap` (UTCTimestamp → first-series value) to position the crosshair on the oscillator chart at the matching time and value (one-directional: price → oscillators). (4) Resizable panes: `DragSplitter` component (5px `cursor:row-resize` divider); `oscPaneHeights: Record<string, number>` state; OscPane accepts `height` prop; ResizeObserver updates both width and height on resize; `MIN_OSC_HEIGHT = 80px`. App.tsx: `useMemo` added for `visibleArtifacts` and `instanceColorMap` to prevent spurious Chart re-renders. Frontend only (no backend changes). 13 new tests: 3 color editor tests in ChartIndicatorPanel (color input renders, color change no-recompute, seriesColors in callback) + 10 Chart.tsx tests (render, OscPane appears, splitter, drag-down height, drag-up clamp, overlay-only no-splitter, empty-artifacts, time-range subscription, crosshair subscription, two groups). Frontend 484 ✓ (+13 from 471), TypeScript clean, backend unchanged. |
| Chart-UX-3C.1 | **Indicator workspace refinement** — Compact sidebar panel replacing large always-expanded forms. New `IndicatorInstance` fields: `instanceLabel` (metadata-driven compact label, e.g. "SMA (20)"), `instanceColor` (palette-assigned per instance), `visible` (hide/show without removal). ChartIndicatorPanel rewritten: header "Indicators (N) + Add Indicator", compact instance rows (swatch · label · eye/settings/remove), collapsed editor by default (open on settings click), color palette (10 distinct colors, assigned sequentially), `Clear All` footer button. App.tsx: sidebar layout shift (ChartIndicatorPanel moved from chart-area to sidebar), `indicatorInstances` state, derives `visibleArtifacts` = filter(visible && artifact !== null) and `instanceColors` map for Chart consumption. Chart.tsx: multi-oscillator pane support (one OscPane per tool_id group below strategy oscillator pane), time-scale sync across all panes, `instanceColors` override backend default_color for single-series overlay tools (SMA20/50/200 now visually distinct), null warmup filtering (not rendered as zero), oscillator pane visibility derived from both overlay AND artifacts. Frontend only (no backend changes). 25 new tests: compact row rendering, collapsed editor, settings/apply/cancel flows, visibility toggle, multi-instance colors, remove/clear-all, onInstancesChange tracking, error states, catalog-mode notice. Frontend 471 ✓ (+5 from 466), TypeScript clean, backend unchanged. |
| Chart-UX-3C | Frontend indicator panel + parameter editor: `frontend/src/types/chartIndicators.ts` (IndicatorInstance, IndicatorArtifactResponse, ChartIndicatorMetadata and related types); `frontend/src/api/chartIndicators.ts` (`getChartIndicators()` public GET, `computeIndicatorArtifact()` authedFetch POST); `frontend/src/components/ChartIndicatorPanel.tsx` (indicator picker with category groups and search by tool_id/display_name/description, no hardcoded list; per-instance parameter editor; Add/Apply/Remove buttons; catalog-mode notice; multi-instance support via counter-based instanceId; `onArtifactsChange` parent callback); `frontend/src/components/Chart.tsx` (new `indicatorArtifacts` prop; `artifactPriceSeriesMapRef` + `artifactOscSeriesMapRef` for independent artifact series lifecycle; null warmup filtering — never zero; oscillator pane visibility derived from both overlay AND artifacts; histogram render_type support); `frontend/src/App.tsx` (imports `ChartIndicatorPanel`, adds `indicatorArtifacts` state, passes `indicatorArtifacts` to `Chart`, clears artifacts on `handleFetch`/`handleCatalogLoad`, key-based panel reset on data context change); `frontend/vite.config.ts` (added `/chart` proxy). 29 new tests: API (8), component (20 — load/categories/search/add/multi-instance/apply/remove/onArtifactsChange/error/catalog-notice/disabled/null-warmup/overlay-oscillator-coexist/meta-error). Frontend 466 ✓ (+29), TypeScript clean, backend unchanged. |
| Chart-UX-3B.1 | Metadata consolidation: eliminated `_CHART_DISPLAY_METADATA` static dict from `chart_indicator_service.py`. Chart ecosystem is now fully registry-driven. Added `ChartSeriesSpec` model to `backend/tools/models.py` (frozen, extra=forbid; series_id/label/pane/render_type/default_color). Added 9 new fields to `ToolMetadata` (chart_pane, chart_render_type, chart_series_kind, chart_category, chart_subcategory, chart_search_keywords, chart_display_order, chart_editable_parameters, chart_output_series). Added `@model_validator(mode="after")` that rejects `visible_on_chart=True` without category=indicator or missing chart metadata (fail-fast at construction). Migrated all 6 built-in tools (sma/ema/rsi/macd/atr/bollinger_bands) to declare full chart metadata inline in ToolMetadata. Refactored `list_chart_indicators()` and `compute_indicator_artifact()` to read directly from registry metadata — no static dict lookups. Added `ChartSeriesSpec` to `backend/tools/__init__.py` exports. 31 new tests: metadata migration (11 — all 6 tools have complete chart_* fields, MACD/BB series IDs align with output_feature_names), registry-driven discovery (3 — no static dict, new tool appears automatically, response reflects ToolMetadata), metadata validation (4 — missing chart_pane raises, missing output_series raises, wrong category raises, False bypasses validation), ChartSeriesSpec model (4 — creation/frozen/extra-forbidden/export). Backend 4 847 ✓ (+31 new), Frontend 437 ✓ unchanged. |
| Chart-UX-3B | Backend indicator artifact endpoint: `GET /chart/indicators` (public, metadata-driven indicator picker) + `POST /chart/indicator-artifact` (auth required, reuses `_TOOL_DISPATCHERS`, timestamp-aligned null warmup values, instance_id echo). Added `visible_on_chart: bool = False` to `ToolMetadata`; set `True` on all 6 built-in tools (sma/ema/rsi/macd/atr/bollinger_bands). New files: `backend/api/schemas/chart.py`, `backend/api/services/chart_indicator_service.py`, `backend/api/routes/chart.py`. Static `_CHART_DISPLAY_METADATA` dict bridges gap until Chart-UX-3D migrates rendering metadata into ToolMetadata. 54 new tests: indicator list (9), artifact auth/validation (3), SMA (8), EMA (3), RSI (3), MACD (5), Bollinger (3), ATR (3), no-data (2), consistency tests (4 — chart artifact values == `compute_tool_outputs_for_history` for SMA/EMA/MACD/Bollinger), visible_on_chart field tests (7). Backend 4 816 ✓ (54 new), Frontend 437 ✓ unchanged. |
| Chart-UX-3A.1 | Contract hardening phase (doc-only): `docs/CHART_INDICATOR_TOOL_CONTRACT.md` updated from 15→20 sections. Added: §4 Tool-Type Taxonomy (indicator/strategy_helper/risk_helper/portfolio_helper/execution_helper/analysis_helper — only `tool_type=indicator` may be chart-visible); §7 Tool Discovery Metadata (category, subcategory, search_keywords, display_order; category enum: Trend/Momentum/Volatility/Volume/Market Structure/Pattern Recognition/Risk/Custom; picker must be metadata-driven, no hardcoded tool lists); §9 Multiple Indicator Instance Support (tool_id vs instance_id separation; same tool, multiple independent instances; instance lifecycle on chart; alignment with `ToolConfiguration` in strategy builder); §10 Indicator Presets (preset_id/name/tool_id/parameters; built-in platform presets for all 6 tools; user-defined presets deferred; presets are convenience wrappers only — no separate computation); §15 Experimental Indicator Promotion Lifecycle (Research Sandbox → Experimental → Validated → Registry Tool → Chart-Visible → Strategy Usage → Backtest/FT/PT; promotion gates for each transition; demotion not standard). Updated §6 to include `tool_type` as required field. Updated §14 onboarding checklist (+6 new items). Added §16.7 (non-indicator tools cannot be chart-visible) and §16.8 (hardcoded picker forbidden). Added §17.7–17.10 test categories. Updated §18 roadmap (3B adds indicator list endpoint and tool_type enforcement; 3C adds multi-instance + preset picker). Updated §20 with new code alignment notes. All section cross-references renumbered (§13→§18 for roadmap). Backend 4 762 ✓ unchanged, Frontend 437 ✓ unchanged. |
| Chart-UX-3A | Architecture/documentation contract phase: `docs/CHART_INDICATOR_TOOL_CONTRACT.md` created. Defines how tools become chart indicators: tool classification taxonomy (overlay/oscillator/multi-series/event/drawing/non-visual), visualization metadata contract (14 fields incl. `visible_on_chart`, `chart_pane`, `render_type`, `output_series`, `warmup_bars_required`), overlay vs pane rules, parameter editing contract, backend computation rule, indicator artifact response shape, future tool onboarding checklist, forbidden patterns (frontend-only formulas, duplicate formulas, hardcoded tool-specific rendering, bypassing normalized data), testing expectations, phased roadmap (3B backend endpoint → 3C frontend panel → 3D tool onboarding → 3E promotion workflow), and architectural alignment to guardrails. No code changes. Backend 4 762 ✓ unchanged, Frontend 437 ✓ unchanged. |
| P8A | Paper Trading Cleanup: fixed stale paper-tested promotion action after refresh by loading current draft lifecycle in `PaperTradingPanel` detail view and only showing the promotion panel while the draft remains `forward_tested`; added `signal_eligible_bars_processed` to `ForwardTestSessionSummaryResponse` and `ForwardTestSessionSummary`; paper create-form FT picker now filters with typed `signal_eligible_bars_processed > 0` instead of relying on an optional hidden field; added `frontend/src/api/__tests__/paperTrading.test.ts` covering active paper API client calls and auth header behavior; strengthened `PaperTradingPanel` tests for zero-evidence FT sessions and already-`paper_tested` refresh state; removed unused `derive_warmup_bars_required` import from paper route; updated route/API comments; validation: backend 4 676 ✓, frontend 317 ✓, `npm run build` ✓; no paper execution/lifecycle-rule/approved-for-live/live-trading behavior changes |
| P3 | Paper Trading UX Foundation: `frontend/src/types/paperTrading.ts` (6 types mirroring backend schemas — `PaperTradingSessionStatus`, `PaperSimulationAssumptions`, `PaperStrategySnapshot`, `PaperTradingSessionSummary`, `PaperTradingSessionDetail`, `PaperAccount`, `CreatePaperTradingSessionRequest`); `frontend/src/api/paperTrading.ts` (4 API functions — `createPaperTradingSession`, `listPaperTradingSessions`, `getPaperTradingSession`, `getPaperTradingAccount`; all use `authedFetch`; no `setInterval`); `frontend/src/components/PaperTradingPanel.tsx` — `ELIGIBLE_PT_STATUSES = new Set(['forward_tested','paper_tested','approved_for_live'])` mirrors backend gate; `StatusBadge` + `AccountSummaryCard` + `SessionDetailView` + `CreateForm` + `SessionList` sub-components; draft picker (status-filtered); optional FT session picker (now filtered by draft ID + typed `signal_eligible_bars_processed > 0` after P8A); lifecycle notice; `pt-panel` / `pt-create-form` / `pt-draft-select` / `pt-no-eligible-drafts` / `pt-ft-session-select` / `pt-lifecycle-notice` / `pt-symbol-input` / `pt-timeframe-select` / `pt-provider-input` / `pt-starting-cash-input` / `pt-create-btn` / `pt-create-error` / `pt-session-list` / `pt-empty-state` / `pt-new-session-btn` / `pt-account-summary` / `pt-session-config` / `pt-list-error` testids; `App.tsx` extended: `'paper-trading'` added to `ActiveView` union, `PaperTradingPanel` imported, "Paper Trading" nav tab, `{activeView === 'paper-trading' && <PaperTradingPanel />}` render block; 11 new frontend tests in `frontend/src/components/__tests__/PaperTradingPanel.test.tsx`; backend unchanged; frontend 268 ✓, `npm run build` ✓ |
| P2 | Paper-trading API hardening: (P2.2) `_PT_ELIGIBLE_STATUSES` narrowed to `{FORWARD_TESTED, PAPER_TESTED, APPROVED_FOR_LIVE}` — `BACKTESTED` removed; error message now explicitly guides toward `forward_tested`; (P2.1/P2.3/P2.4) `forward_test_session_id` validation added to `create_session` — UUID format check (400), existence+ownership via `ForwardTestRepository.load(owner_id)` (404, information hiding), `ft_session.draft_id == request.draft_id` linkage check (422), `ft_session.signal_eligible_bars_processed >= 1` evidence rule (422); `get_forward_test_repository` DI dependency added to route; `ForwardTestSessionNotFoundError` imported from `backend.forward_testing.exceptions`; (P2.5) atomicity fix — account now saved before session; if account save fails, session is never written (no orphan session); prior order was session-first which left orphan sessions on account save failure; audit event extended with `forward_test_session_id` field; (P2.6) all error messages are actionable and permission-safe; 11 new backend tests in `TestCreateSessionP2` class (ft_session_not_found, wrong_owner, draft_mismatch, no_evidence, valid_creates_session, id_optional_none, id_stored_in_detail, atomicity_orphan, lifecycle_error_message, cross_user_draft, backtested_rejected) + 1 added to existing `TestCreateSession` class; no frontend changes; backend 4 601 ✓ |
| P1 | Forward-test evidence promotion: `promote_draft_to_forward_tested(session_id, draft_id, ft_repository, draft_repository, owner_id, *, notes)` added to `backend/api/services/draft_service.py`; `ForwardTestPromotionError` exception class added alongside `LifecyclePromotionError`; evidence rule: `session.signal_eligible_bars_processed > 0` (warmup completed + at least one live bar evaluated, regardless of signal generation); checks session→draft binding (`session.draft_id == draft_id`), draft must be at BACKTESTED, calls `validate_lifecycle_transition(BACKTESTED, FORWARD_TESTED)`, mutates draft, emits `GOV_PROMOTION_REQUESTED` with `bars_evaluated` + `signals_recorded` in details; `PromoteDraftToForwardTestedRequest(BaseModel, extra="forbid")` schema added to `backend/api/schemas/forward_testing.py` (`draft_id: str`, `notes: str | None`); `POST /forward-tests/{session_id}/promote-draft` route added to `backend/api/routes/forward_testing.py` — maps `ForwardTestSessionNotFoundError`→404, `DraftNotFoundError`→404, `ForwardTestPromotionError`→422, `ValueError`→422; frontend: `promoteDraftToForwardTested(sessionId, draftId, notes?) → Promise<StrategyDraftData>` added to `frontend/src/api/forwardTests.ts`; `SessionOperatorView` in `ForwardTestPanel.tsx` gains promotion panel (`ft-promotion-panel`) shown when `lifecycle_status_at_activation === 'backtested'`: `ft-promotion-no-evidence` when `signal_eligible_bars_processed === 0`, else `promote-to-forward-tested-btn`; `ft-promotion-success` on success; `ft-promotion-error` surfaces backend errors; `ft-promotion-ineligible` shown when draft was already beyond backtested at session activation; no paper-trading UI added; 9 backend tests + 7 frontend tests; backend 4 590 ✓, frontend 257 ✓, `npm run build` ✓ |
| P0 | Lifecycle integrity repair: (P0.1) `validate_draft()` in `draft_composition_service.py` now promotes `DRAFT→VALIDATED` on valid toolset — persists update, emits `GOV_PROMOTION_REQUESTED`, returns `lifecycle_promoted=True` in `CompositionValidationResponse`; already-VALIDATED/BACKTESTED states not downgraded; failed validation does not promote; (P0.2) `lifecycle_status` removed from both `DraftCreateRequest` and `DraftUpdateRequest` (both have `extra="forbid"` so passing field → HTTP 422); dead lifecycle-bypass block removed from `update_draft()`; `create_draft()` now hardcodes `lifecycle_status=DRAFT`; stale `test_draft_update_request_lifecycle_status_is_optional` replaced with `test_draft_update_request_lifecycle_status_is_absent`; (P0.3) `BacktestReportPage` promotion panel restricted to `lifecycleAtRun === 'validated'` only (was `!BACKTESTED_OR_BEYOND.has(...)` which incorrectly included `'draft'`); `draft-prerequisite-notice` panel added for `lifecycleAtRun === 'draft'` with guidance to validate first; (P0.4) `/forward-tests` + `/paper-trading` added to `frontend/vite.config.ts` proxy; (P0.5) `REPOSITORY_STATE.md` cleaned of stale references to deleted R8 files; 11 new backend tests (6 promotion + 5 API integrity) + 5 new frontend tests; backend 4 646 ✓, frontend 250 ✓, `npm run build` ✓ |
| R8 | Legacy frontend cleanup audit: confirmed active app path uses `StrategyTestPanel` → `compositionRun` + `backtestRuns`, `BacktestReportPage`, `BacktestHistoryPanel`, `ForwardTestPanel`; removed confidently dead frontend artifacts: `components/BacktestPanel.tsx`, `api/backtest.ts`, `types/backtest.ts`, `api/strategyRuns.ts`, `components/ToolPanel.tsx`, `components/ToolSetPanel.tsx`, `components/ConfiguredToolList.tsx`; retained `compositionRun.ts`, `backtestRuns.ts`, report/history/forward-test/composer components as active; moved chart indicator typing to `types/toolVisualization.ts` via active `compositionRun` response typing; validation: `rg` import checks clean for removed paths, `cd frontend && npm run build` ✓, `npm test` 245 ✓; no backend/lifecycle/forward-test/paper-trading behavior changes |
| R3 | Backend lifecycle promotion repair: `promote_draft_to_backtested()` in `draft_service.py` — validates draft ownership, loads backtest report (validates run ownership + `run.draft_id == draft_id` + `run.status == "completed"`), calls `validate_lifecycle_transition(current, BACKTESTED)`, updates draft, emits `GOV_PROMOTION_REQUESTED`; `LifecyclePromotionError` exception class added; `PromoteDraftRequest` schema added to `backtest_runs.py` schemas; `POST /backtests/runs/{run_id}/promote-draft` route added (thin: no evidence logic in route); forward-test lifecycle gate unmodified (VALIDATED→FORWARD_TESTED still forbidden); 17 new tests in `tests/unit/test_lifecycle_promotion.py` (9 service + 8 route); backend 4 635 total |
| R2 | Composer tool selection repair: root cause was New Draft UI accepting friendly/non-UUID IDs while draft path routes now reject non-UUID IDs with 400; after create, `DraftWorkspace` tried `GET /drafts/{friendly_id}`, failed, and never mounted the Toolset/Add Tool selector. Tool registry/API were healthy (`atr`, `bollinger_bands`, `ema`, `macd`, `rsi`, `sma`). Fix: new composer creation now preserves valid UUID inputs and otherwise generates a UUID-backed draft/toolset ID before create/select; added `DraftWorkspace` regression proving a `test1` input still opens the new composer and exposes registered tools. Validation: frontend build ✓, focused DraftWorkspace test ✓, full frontend 184 ✓, backend tools/draft-composition/hardening 79 ✓; no backend/API/paper-trading changes |
| R1 | Frontend production build repair: root cause was production `tsc` including `src/**/*.test.*` fixtures via `frontend/tsconfig.json` `include: ["src"]`; fixed by excluding test/spec files from production typecheck, adding `npm test` script for existing Vitest runner, and tightening forward-test/auth test fixtures (`subscription_expires_at`, `ForwardTestSessionStatus`, removed unused mocks/import); validation: `cd frontend && npm run build` ✓, focused forward-test tests 28 ✓, `npm test` 183 ✓; no product behavior/API/backend changes |
| 4D | Paper trading architecture review: `docs/PAPER_TRADING_IMPLEMENTATION_REVIEW.md` produced; full codebase survey against `PAPER_TRADING_ARCHITECTURE.md`, `EXECUTION_CONTRACT.md`, `EXECUTION_AUDIT_MODEL.md`, `STRATEGY_PROMOTION_LIFECYCLE.md`; inventoried 10 directly reusable FT components (evaluate_history, compile_semantics, StrategySnapshot seal, OHLCVService, ProviderAdapterFactory, ForwardTestBarStore, ForwardTestSignalStore, AuditEvent infrastructure, ownership enforcement, _prepare_strategy); identified 18 missing components (PaperTradingSession, SimulationAssumptions, PaperAccount, PaperOrder, PaperFill, PaperPosition, AccountStateSnapshot, PaperBrokerAdapter, ExecutionGateway, PaperTradingRepository, 3 stores, PaperAccountStore, metrics calculator, 29 PT_* AuditEventKind values, API routes, schemas, PaperTradingPanel); phased roadmap 4E.1–4E.6 defined; readiness rating **A−** (architecture complete, implementation complexity manageable, `next_bar_open` cross-bar state and `PAPER_TESTED` enum presence flagged for pre-implementation verification); no code changes |
| 4C.6 | Forward testing integration validation: `tests/integration/test_forward_testing_integration.py` — 65 tests across 10 sections; all 13 validation objectives passed; 4 test defects fixed (sessions missing semantics in poll-path tests); no production defects found; integration readiness rating **B** (all Phase 4C objectives met; known limitations: log-only audit, JSON-backed storage, no holiday calendar, no FT_SESSION_COMPLETED route in Phase 4C scope); test counts: backend 4 188 total |
| 4E.4 | Paper Trading API routes + schemas: `backend/api/routes/paper_trading.py` — 14 protected routes under `/paper-trading/sessions` (POST create, GET list, GET detail, POST run-cycle, POST pause, POST resume, POST terminate, GET signals, GET orders, GET fills, GET positions, GET account, GET equity-curve, GET bars); all require `require_active_subscription`; `user_id` always from JWT; wrong-owner → 404; session creation seals `PaperStrategySnapshot` (SHA-256 hash), builds `SimulationAssumptions` from request body, creates `PaperAccount` atomically, emits `PT_SESSION_CREATED`; run-cycle rejects catalog mode (422), resolves vault credential, builds provider + `DatasetIdentity`, constructs `PaperTradingService` from 10 DI dependencies, calls `service.run_cycle()`; lifecycle transitions (pause/resume/terminate) validated via `validate_pt_session_transition()`; `backend/api/schemas/paper_trading.py` — `SimulationAssumptionsRequest/Response`, `CreatePaperTradingSessionRequest`, `PaperStrategySnapshotResponse`, `PaperTradingSessionSummaryResponse`/`DetailResponse` (no `strategy_json`, no `user_id`, no `file_path`), `PaperCycleResultResponse` (21 fields including `drawdown_stop_triggered`), `PaperOrderResponse`/`PaperFillResponse`/`PaperPositionResponse`/`PaperAccountResponse`/`AccountStateSnapshotResponse`/`PaperBarResponse`/`PaperSignalResponse`; router registered in `backend/api/main.py`; 45 new tests covering create (8), list (4), detail (4), run-cycle (4), pause/resume/terminate (9), signals/orders/fills/positions/account/equity-curve/bars (14), security (2) |
| 4E.3 | PaperTradingService execution engine: `backend/paper_trading/service.py` — `PaperCycleResult` frozen dataclass (21 fields); `PaperTradingService` with 10-dependency DI constructor; `run_cycle()` dispatches PENDING→`_activate()` / RUNNING→`_poll_cycle()` / PAUSED/terminal→no-op; `_activate()` derives warmup from toolset, fetches warmup bars, PENDING→RUNNING, emits `PT_SESSION_ACTIVATED`; `_poll_cycle()` resolves NEXT_BAR_OPEN pending orders at bar.open (mandatory pre-signal), fetches new bars via `get_bars_since()`, full-window recomputation, signal evaluation (entry/exit long only), SBC fills immediately / NBO creates pending, mark-to-market every bar, drawdown stop check, `AccountStateSnapshot` per signal-eligible bar, cursor advance; `_apply_buy_fill` (open/scale position; deduct cash), `_apply_sell_fill` (close/reduce position; add cash; realize PnL), `_mark_to_market` (all open positions updated), `_resolve_pending_order` (fill/cancel/reject); module helpers: `_compute_drawdown()`, `_get_triggered_rule_id()`; all PT_* audit events emitted; no file_path/secrets in audit; 69 new tests; bug fixed: removed stale `warmup_bars_required=0` kwarg from activation denial return path |
| 4E.2 | Paper trading execution layer: `backend/paper_trading/execution_models.py` — 4 enums (`OrderDirection`, `PaperOrderStatus`, `ExecutionReason`, `RejectionReason`) + 3 frozen models (`PaperOrder`, `PaperFill`, `PaperPosition`; all UUID-guarded, UTC-enforced, extra=forbid); `backend/paper_trading/execution_stores.py` — `PaperOrderStore` (pending/filled/cancelled/rejected per session; `mark_filled` atomic 3-write: pending remove → filled append → fill inline write; dedup by fill_id; `list_*` owner-filtered), `PaperFillStore` (append-only, dedup, count, owner filter), `PaperPositionStore` (one file per position, `save`/`update`/`load`/`list_positions`/`list_open_positions`/`count_open`); `backend/paper_trading/broker_adapter.py` — `PaperBrokerAdapter` (all @staticmethod: `compute_fill_price` / `compute_fee` / `compute_quantity` / `create_pending_order` / `build_fill` / `reject_order`; quantity guard for zero/negative on reject; no state, no store mutation, no account mutation); 3 DI factories added to `dependencies.py` (`get_paper_order_store`, `get_paper_fill_store`, `get_paper_position_store`); `exceptions.py` extended with `PaperOrderNotFoundError` / `PaperPositionNotFoundError` / `PaperPositionAlreadyExistsError`; 175 new tests; no routes, no service, no account mutation, no scheduler |
| 4E.1 | Paper trading foundation: `backend/paper_trading/` package — `exceptions.py` (6 typed exceptions: `PaperTradingSessionNotFoundError`, `PaperTradingSessionAlreadyExistsError`, `PaperTradingPersistenceError`, `PaperTradingInvalidTransitionError`, `PaperAccountNotFoundError`, `PaperAccountAlreadyExistsError`); `models.py` — `PaperTradingSessionStatus` 6-state enum + transition table + `validate_pt_session_transition()` + `is_pt_terminal_status()`, `FillTimingModel` / `FeeMode` / `SlippageMode` / `PositionSizeMode` enums, `SimulationAssumptions` (frozen, 11 fields, declared execution mechanics), `PaperStrategySnapshot` (frozen, sealed at session creation, independent from FT boundary), `PaperTradingSession` (frozen, source_mode consistency + snapshot_hash consistency validators, UUID field guards, UTC enforcement), `PaperAccount` (frozen, 1:1 session binding, currency safe-string validation), `AccountStateSnapshot` (frozen, append-only equity curve point); `repository.py` — `PaperTradingRepository` (JSON-backed, ownership enforcement, wrong-owner → `PaperTradingSessionNotFoundError`, UUID path guard, `save`/`load`/`update`/`list_all`/`list_active`/`exists`); `stores.py` — `PaperAccountStore` (1:1 enforcement, `save`/`load_by_session_id`/`update`/`exists_for_session`/`load_by_account_id`) + `AccountStateSnapshotStore` (append-only, dedup by bar_timestamp, `append`/`list_snapshots`/`count`); `settings.paper_trading_storage_path` added to config; 3 DI factories added to `dependencies.py` (`get_paper_trading_repository`, `get_paper_account_store`, `get_account_state_snapshot_store`); 30 `PT_*` audit event kinds added to `audit.py` (10 session lifecycle + 2 activation gate + 3 order + 1 fill + 4 position + 3 account + 5 data + 2 access); `test_security_baseline.py` updated; 141 new tests (55 models + 46 repository + 40 stores); NO routes, NO service, NO execution, NO broker, NO frontend, NO scheduler |
| 4D | Paper trading architecture review: `docs/PAPER_TRADING_IMPLEMENTATION_REVIEW.md` produced; 18 sections; 10 reusable components; 18 missing components; model recommendations for session/account/order/fill/position/portfolio; PT_* audit taxonomy; 14 API routes workflow; 6 risks; 4E.1–4E.6 roadmap; readiness A−; no code |
| 4C.5 | Forward testing API routes + frontend panel: 9 routes under `/forward-tests` (create, list, get, run-cycle, pause, resume, terminate, signals, bars); all require `require_active_subscription`; snapshot seal + SHA-256 hash + warmup derivation at creation; catalog mode rejected on run-cycle (422); `ForwardTestPanel.tsx` with status badges + action buttons + signal drill-in + create form; "Forward Test" nav tab; 37 backend + 28 frontend tests |
| 4C.4 | ForwardTestService single-cycle evaluation engine: `backend/forward_testing/service.py` — `CycleResult` frozen dataclass; `run_cycle()` with PENDING→`_activate()` + RUNNING→`_poll_cycle()` + PAUSED/terminal no-op; `_activate()` fetches warmup bars via `get_recent_bars()`, stores with `is_warmup_bar=True`, sets cursor, transitions session to RUNNING, emits `FT_SESSION_ACTIVATED`; `_poll_cycle()` uses `get_bars_since(cursor)` + gap detection (`is_bar_expected` calendar check) + full-window recomputation (`compute_tool_outputs_for_history()`) + `evaluate_history()` + idempotent signal/bar persistence + cursor advance + session counter updates + emits `FT_POLL_COMPLETED` / `FT_GAP_DETECTED` / `FT_SIGNAL_GENERATED` / `FT_SIGNAL_SUPPRESSED`; `_prepare_strategy()` deserializes + compiles `StrategySemantics`; `RangeProviderAdapter` imported only under `TYPE_CHECKING` guard (boundary: `forward_testing` ≠ `data_providers`); no scheduler, no polling loop, no API routes, no broker execution; 41 new tests |
| 4C.3A | Exchange calendar & market session finalization policy: `backend/market_calendar/` — `TradingCalendar` ABC; `TwentyFourSevenCalendar` + `WeekdayMarketCalendar` + `DefaultCalendar`; `get_calendar()` registry; `is_bar_expected()`; `is_bar_finalized()` calendar-aware; 60 new tests |
| 4C.3 | OHLCVService forward-testing extensions: `timeframe_to_timedelta()` (all 15 canonical timeframes); `is_bar_finalized()` (candle close + safety buffer, UTC-aware, raises on naive datetimes); `OHLCVService.get_recent_bars()` (latest N finalized bars, BYPASS_CACHE); `OHLCVService.get_bars_since()` (finalized bars strictly after cursor, BYPASS_CACHE); `settings.forward_test_bar_finalization_buffer_seconds = 60` added to config; normalizer enforces monotonic timestamps (stub providers must supply ascending bars); no session coupling, no storage writes; 44 new tests |
| 4C.2 | Forward testing lifecycle + audit taxonomy integration: `StrategyLifecycleStatus.FORWARD_TESTED` added to `lifecycle.py`; canonical promotion path `backtested→forward_tested→paper_tested` established; `backtested→paper_tested` retained (deprecated transitional per STRATEGY_PROMOTION_LIFECYCLE.md §5); `forward_tested→backtested` rollback allowed; 19 `FT_*` + 9 `GOV_*` audit event kinds added to `audit.py`; `AuditEvent.correlation_id: str | None` added (optional, backward-compat, serializes in JSON when present); existing audit callers unaffected; `ForwardTestSessionStatus` vocabulary mapping documented (pending=created, terminated=stopped); 95 new tests; no API routes, no scheduler, no strategy evaluation |
| 4C.1 | Forward testing foundation: `backend/forward_testing/` package — `exceptions.py` (4 exception classes), `models.py` (`ForwardTestSessionStatus` 6-state enum + transition table + `StrategySnapshot` + `ForwardTestSession` + `ForwardTestSignal` + `ForwardTestBar`), `repository.py` (`ForwardTestRepository` — JSON-backed, ownership enforcement, UUID path guard, wrong-owner → same error as not-found, `save`/`load`/`update`/`list_all`/`list_active`/`exists`), `stores.py` (`ForwardTestSignalStore` + `ForwardTestBarStore` — idempotent append, ordering, last-timestamp queries); `settings.forward_test_sessions_storage_path` added to `backend/core/config.py`; 3 DI factories in `backend/api/dependencies.py`; 125 new tests (models: 51, repository: 30, stores: 29, plus idempotency datetime-comparison fix — Pydantic JSON serializes UTC as `Z` vs Python `.isoformat()` as `+00:00`, fixed by parsing stored timestamps via `datetime.fromisoformat()` before comparison); no API routes, no scheduler, no strategy evaluation |
| 4B | Forward testing implementation review: `docs/FORWARD_TESTING_IMPLEMENTATION_REVIEW.md` produced; full codebase survey against 4A architecture documents; identified directly reusable components (`evaluate_history()`, `ProviderAdapterFactory`, `DraftRepository` pattern, `require_active_subscription`, `compile_semantics()`); identified extension targets (`OHLCVService` needs 2 new methods, `AuditEventKind` needs FT_/GOV_ events, `AuditEvent` needs `correlation_id`, `StrategyLifecycleStatus` needs `forward_tested`); identified 8 missing components (`ForwardTestSession`, `ForwardTestRepository`, `ForwardTestService`, `ForwardTestSignalStore`, `ForwardTestBarStore`, 7 API routes, frontend panel, settings path); confirmed `backend/forward_testing/` + `backend/jobs/` + `backend/execution/` are empty placeholders; audit is log-only (no persistence, no execution taxonomy); lifecycle missing `forward_tested`/`LIVE`/`REVOKED`; phased roadmap 4C.1–4C.6 defined; no code changes |
| 4A.5 | Strategy promotion lifecycle: `docs/STRATEGY_PROMOTION_LIFECYCLE.md` established; authoritative lifecycle with 9 states (DRAFT → VALIDATED → BACKTESTED → FORWARD_TESTED → PAPER_TESTED → APPROVED_FOR_LIVE → LIVE → REVOKED → ARCHIVED); 6 evidence categories; per-gate evidence requirements (§7–§9); no numerical thresholds — evidence completeness + human review; self-promotion prohibited; `GOV_STRATEGY_APPROVED_FOR_LIVE` requires `explicit_acknowledgment_text`; revocation model from LIVE/APPROVED_FOR_LIVE/PAPER_TESTED; backward-compatibility alignment with current 6-state implementation; `forward_tested`, `live`, `revoked` states defined as future additions; promotion readiness philosophy established; 9 non-negotiable constraints |
| 4A.4 | Execution audit model: `docs/EXECUTION_AUDIT_MODEL.md` established; authoritative audit event taxonomy across all execution subsystems; 6 audit categories (`FT_`, `PT_`, `LT_`, `GOV_`, `ADMIN_`, `FAILURE_`); 13-field common audit record envelope with `correlation_id`; 19 FT_ events + 22 PT_ events + 8 LT_ conceptual events + 8 GOV_ events with `explicit_acknowledgment_text` on `GOV_STRATEGY_APPROVED_FOR_LIVE`; immutability model (append-only, corrections via new records); 4 retention tiers (governance + live fills = permanent); 4 review types; 7 query patterns; three-part evidence base for live trading authorization; LT_ extension path defined; 7 non-negotiable constraints |
| 4A.3 | Paper trading architecture: `docs/PAPER_TRADING_ARCHITECTURE.md` established; defines `PaperTradingSession` (extends `ForwardTestSession`), `PaperAccount`, `PaperPosition`, `PaperOrder`, `PaperFill` models; execution intent flow through `PaperBrokerAdapter`; fill simulation philosophy (market orders, 2 fill timing models, declared slippage/fee, 3 sizing modes); account state ownership rules; equity curve model; 20+ audit event categories; drawdown stop mechanism; relationship to forward testing and live trading; 19 non-negotiable constraints |
| 4A.2 | Forward testing architecture: `docs/FORWARD_TESTING_ARCHITECTURE.md` established; defines `ForwardTestSession` model, session lifecycle state machine, REST-polling data acquisition model, bar finalization and unseen-bar detection, per-bar evaluation reusing existing engine, `ForwardTestSignal` recording contract, provenance/ownership/persistence/audit/failure models, UI workflow concept, polling→streaming upgrade path, relationship to paper trading and promotion lifecycle documents |
| 4A.1 | Execution contract architecture: `docs/EXECUTION_CONTRACT.md` established as foundational contract for all future execution subsystems (forward testing, paper trading, live trading); defines execution philosophy, 5 execution modes, 7 core objects, session model, gateway contract, state management principles, determinism rules, ownership/provenance/audit requirements, 12 safety constraints, relationships to existing architecture, and 4 future documents |
| 3S-D | Legacy route decommissioning & path safety hardening: `/datasets/*` + `POST /strategy-runs/run` gated with auth; `validate_uuid_id()` added to `request_validation.py`; UUID enforcement on all 4 `{run_id}` + 4 `{draft_id}` path-param routes; `POST /semantics/validate` + `POST /tools/validate-toolset` protected; `GET /tools` + `GET /market-data/providers` documented as intentionally public; frontend `semantics.ts` + `strategyRuns.ts` switched to `authedFetch`; 36 new hardening tests; 35 regression tests updated to use valid UUIDs; TypeScript fix |
| 3S-C | Research provenance & workflow continuity: `GET /backtests/runs` history API, `BacktestHistoryPanel`, dataset/draft provenance snapshots, `useSessionPersistence`, "Resume Last Report", Run Provenance section in report; 48 new tests |
| 3S-B | Security & runtime boundary fixes: compute endpoint auth, payload guards, lifecycle state machine, Polygon ENV gate, storage path DI, dependency consolidation |
| 3R | Research workflow UX (ResearchSession, SessionProvenanceStrip, report→Composer, Report nav tab) |
| 3Q | Operational documentation refactor (HANDOFF.md compression, archive) |
| 3P-E | Dataset catalog UX (CatalogManager, Source Mode B, file_path safety) |
| 3P-D | Superadmin role tier, promote/demote governance |
| 3P-C | Lazy subscription expiry enforcement |
| 3P-B.1 | Governance safety: expiry workflow, self-suspension guard, last-admin protection |
| 3P-B | Admin Console browser UI |
| 3P-A.1 | Admin entitlement separation (`has_platform_access`) |
| 3P-A | Subscription eligibility & admin approval foundation |
| 3O | Credential-aware market data UX (Polygon + vault) |
| 3N | Frontend credential management UI |
| 3M.1 | Browser auth & ownership validation (4 bugs fixed) |
| 3M | Frontend ownership integration (`authedFetch` everywhere) |
| 3L | User ownership & resource scoping (user_id from JWT) |
| 3J | Provider credential resolver (vault→polygon) |
| 3I | User provider credential vault (Fernet encryption) |
| 3H | Auth & user identity (JWT, bcrypt, UserRepository) |
| 3G | Polygon.io provider (15 timeframes, pagination) |
| 3F | Security baseline (credentials, audit, request validation) |
| 3E | Dataset catalog & file path resolution |
| 3D | Local CSV/Parquet providers |
| 3C | Dataset cache & storage architecture |
| 3B | Dataset fetch identity (SHA-256 fingerprint) |
| 3A | Provider abstraction layer (ProviderAdapterFactory) |
| 2T–2U | Tool output visualization (two-pane chart), 6-tool registry |
| 2N–2S | Full backtest pipeline (simulation, costs, sizing, report, exports) |
| 2J–2M | Strategy semantic & evaluation engine |
| 2D–2I | Strategy tools builder layer (drafts, toolset, composition) |
| 2A–2C | Core data schemas, storage, OHLCVService |

Full phase detail: `agent/archive/HANDOFF_HISTORY.md`

---

# Recommended Next Phases

**Immediate:**
- **Phase 2V — Custom Research Tools**: First non-standard indicator (swing high/low, divergence, custom feature tool); must register in `_TOOL_DISPATCHERS`, expose `output_feature_names`, respect warmup/lookahead enforcement; marks start of QuantLab-specific research layer
- **Bollinger Bands visualization**: Backend implementation complete; frontend overlay rendering not yet wired

**Near-term:**
- **Phase 4E.2 — PaperBrokerAdapter + Fill Simulation**: `PaperOrder`, `PaperFill`, `PaperPosition` models; `PaperBrokerAdapter.submit_order()` → fill simulation with fee/slippage/sizing; `SIGNAL_BAR_CLOSE` and `NEXT_BAR_OPEN` fill timing; drawdown stop trigger; foundation for Phase 4E.3 service layer
- **Payment / Subscription Integration**: Webhook-driven approval flow; connect `validate_future_expiry` / `approve_user` to a payment provider
- **PostgreSQL migration**: Replace JSON-backed `UserRepository`, `CredentialRepository`, `DraftRepository` with PostgreSQL when multi-instance or durability requirements emerge

**Deferred:**
- Live trading infrastructure (WebSocket, tick streaming)
- Broker adapters (IBKR, Binance)
- Distributed execution / microservices
- Per-candle gap detection in OHLCVService
- Instrument master database
- Audit persistence (DB or event store; currently log-only)
- Holiday calendar (currently weekday/24-7 only)
- Alternative / astronomical datasets

---

# Strategic Timeframe Roadmap

## Current Validated Target

Phase 4C validated the full Research → Backtest → Forward Test → Signal workflow at the following timeframes:

| Timeframe | Status |
|-----------|--------|
| Daily (1d) | ✅ Validated |
| 4-Hour (4h) | ✅ Validated |
| 1-Hour (1h) | ✅ Validated |
| 30-Minute (30m) | ✅ Validated |
| 15-Minute (15m) | ✅ Validated |

These timeframes work with the current single-cycle polling model and full-window recomputation architecture without modification.

## Phase 4H — Intraday Engine Optimization (5m)

**Status:** Planned — after Paper Trading maturity (Phase 4D+)

**Objective:** Enable production-grade 5-minute strategy support.

At 5-minute timeframes, the full-window recomputation model accumulates sessions with hundreds of bars per day. A long-running forward test session will require engine-level optimization to remain performant.

**Required capabilities:**

- **ToolStateSnapshot architecture**: persist intermediate indicator state so each poll cycle computes only the incremental delta rather than the full history
- **Incremental indicator computation**: SMA/EMA/RSI/MACD/ATR/Bollinger Bands must support state-resumption from a snapshot
- **Incremental evaluator execution**: `evaluate_history()` must support evaluating only new bars using persisted rule state from prior bars
- **Session pagination**: `list_bars` / `list_signals` API routes must support cursor-based pagination as bar counts grow into the thousands
- **Provider polling optimization**: `get_bars_since()` fetch windows must be bounded; avoid unbounded lookback as session age grows
- **Long-running session validation**: integration tests covering sessions with 500+ bars

**Outcome:** 5m forward testing; 5m paper trading

## Phase 5A — Realtime Market Data & Streaming Architecture (1m)

**Status:** Planned — after Phase 4H

**Objective:** Enable production-grade 1-minute strategy support.

At 1-minute timeframes, client-driven polling is no longer viable. Bar windows are too short for reliable round-trip latency, and polling introduces systematic latency that degrades signal quality. A streaming ingestion layer is required.

**Required capabilities:**

- **WebSocket ingestion layer**: persistent WebSocket connection to market data providers; per-symbol subscription management
- **Event-driven market data pipeline**: bar events emitted on close; downstream consumers decouple from transport layer
- **Reconnect / recovery logic**: automatic reconnect with missed-bar detection on reconnect; gap audit on resume
- **Event deduplication**: idempotent event processing; duplicate bars from reconnect storms must not produce duplicate signals or store entries
- **Late-bar handling**: bars arriving after the finalization window must be flagged, not silently discarded; audit event emitted
- **Persistent event storage**: streaming bar events must be persisted durably (not in-memory) to survive service restarts without data loss
- **Streaming audit integration**: all stream lifecycle events (`STREAM_CONNECTED`, `STREAM_DISCONNECTED`, `STREAM_RECONNECTED`, `STREAM_GAP_DETECTED`, `STREAM_LATE_BAR`) must emit structured audit events

**Outcome:** 1m forward testing; 1m paper trading; foundation for live execution layer

---

# Operational Rules

1. Read `WORKFLOW_AGENT.md` + `ARCHITECTURE_GUARDRAILS.md` before any implementation session
2. Do not invent scope — all work originates from structured directives
3. Never merge responsibilities across architecture boundaries (API ≠ strategy ≠ provider ≠ storage ≠ frontend)
4. Update `HANDOFF.md` (latest completed work) + `TASKS.md` (active phase) at the end of every session
5. If architecture violation discovered: document in HANDOFF.md, do not silently build on top of it
6. High-impact architecture changes require external (orchestration-layer) approval before implementation
